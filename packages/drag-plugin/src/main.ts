// ── Factory ──
export {dragPlugin, DragPlugin} from './drag';

// ── Constraint utils ──
export {applyConstraint, constrainDelta, snapToGrid, clampToBounds} from './constraint';

// ── Types ──
export type {DragPluginOptions, DragConstraint, DragSnapshot, DragStartEvent, DragMoveEvent, DragEndEvent, DragCancelEvent} from './types';
